import 'package:flutter_test/flutter_test.dart';
import 'package:javier_website/core/utils.dart';

void main() {
  group('Utils', () {
    test('Utils class has required methods', () {
      // Verify Utils class exists and is accessible
      expect(Utils, isNotNull);
    });
  });
}
